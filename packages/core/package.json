{
  "name": "@stele/core",
  "version": "0.1.0",
  "description": "Core parsing, validation, manifest, and generation contracts for Stele.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node --eval \"console.log('Scaffold build placeholder for @stele/core: source files arrive in later tasks.')\"",
    "test": "vitest run --passWithNoTests",
    "typecheck": "node --eval \"console.log('Scaffold typecheck placeholder for @stele/core: no TypeScript sources yet.')\"",
    "lint": "node --eval \"console.log('Scaffold lint placeholder for @stele/core: no lintable sources yet.')\""
  }
}
