# /stele:explain

Explain a specific invariant, checker-backed rule, or generated test target by running:

```bash
stele explain <id>
```

Use the CLI output to answer with the exact invariant source, generated test path, dependencies, rationale, and checker linkage for the requested ID.
