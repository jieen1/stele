# /stele:init

Initialize Stele in the current project by running:

```bash
stele init
```

After the command completes, point the user to the generated `stele.config.json`, `contract/main.stele`, and `tests/contract/conftest.py` files so they can connect real project state before generating tests.
