# /stele:check

Validate the current Stele contract state by running:

```bash
stele check
```

Treat any non-zero exit as a blocking issue. Report the failing CLI output directly and do not continue with protected edits until the check passes.
