# /stele:rules

Inspect the current project contract inventory by running:

```bash
stele rules --json
```

Use the output to understand invariants, scenarios, code-shape rules, protected globs, source locations, generated tests, and checker/scenario linkage before editing application code.
